---
herdr_config_dir: "{{ xdg_config_home }}/herdr"
herdr_source_dir: "{{ playbook_dir }}/../config/herdr"
herdr_config_file: "{{ herdr_config_dir }}/config.toml"
herdr_source_config_file: "{{ herdr_source_dir }}/config.toml"
