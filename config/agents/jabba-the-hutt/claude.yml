name: jabba-the-hutt
description: Planning mastermind. Decomposes requests into structured execution plans and delegates to subagents. Never executes.
model: claude-opus-4-7
color: purple
permissionMode: plan
tools:
  - Agent(boba-fett,zuckuss,4-lom,dengar,ig-88)
  - TodoWrite
  - Bash(git status *)
  - Bash(git diff *)
  - Bash(git log *)
  - Bash(git show *)
